{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "^2.2.0",
    "typescript": "^4.1.2",
    "typescript-snapshots-plugin": "^1.7.0",
    "wsrun": "^5.2.4"
  },
  "scripts": {
    "g:tsc": "tsc",
    "build": "wsrun -c build",
    "dev": "wsrun -c dev"
  }
}
