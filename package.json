{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "^2.2.0",
    "ts-node": "^9.1.0",
    "typescript": "^4.1.2",
    "typescript-snapshots-plugin": "^1.7.0"
  },
  "scripts": {
    "g:tsc": "cd $INIT_CWD && tsc",
    "g:ts-node": "cd $INIT_CWD && ts-node"
  }
}
