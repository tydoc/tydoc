{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "wsrun": "^5.2.4",
    "prettier": "^2.2.0",
    "typescript": "^4.1.2",
    "typescript-snapshots-plugin": "^1.7.0"
  },
  "scripts": {
    "g:tsc": "tsc",
    "build": "wsrun -c build",
    "dev": "wsrun -c dev"
  }
}
